# Hyunglae Lee User Configuration
# ASU NCHR Lab Professor
extends: "_defaults.yaml"

user_name: "Hyunglae Lee"
role: "professor"
show_in_html: true

# === 디바이스 설정 ===
device_id: "HOLOLENS-G4NTC8"
device_ip: "192.168.10.128"

# === 서버 연결 설정 (ASU) ===
server:
  ros_server: "nchr_xarm5"
  ros_host: "192.168.1.100"
  ros_port: 10000

# === 연결 모드 ===
streaming_mode: "zmq_local"

# === RILEY AI 설정 ===
riley:
  mode: "http_ngrok"  # GPU 서버 사용
  personas:
    - basic
    - robot_expert

# === 권한 설정 ===
permissions:
  can_spawn_robot: true
  can_delete_robot: true
  max_robots: 5

  can_spawn_virtual: true
  can_spawn_guide: true

  can_remote_manipulation: false
  can_control_all: false

  admin_access: false

# === 접근 가능 QR/로봇 (ASU 로봇) ===
accessible_qr: ["Q1", "Q2"]  # xarm5, iiwa14
