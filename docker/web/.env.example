# Multi-HRI Web Server 환경 설정
#
# 이 파일을 .env로 복사 후 로컬 경로 수정
# cp .env.example .env
#
# Windows 경로 예시 (슬래시 사용):
#   D:/Data_AR/dev/multi-hri-mr/shared_data
#
# Linux/Mac 경로 예시:
#   /home/user/multi-hri-mr/shared_data

# shared_data 경로 (web_panel, config, server 포함)
SHARED_DATA_PATH=D:/Data_AR/dev/multi-hri-mr/shared_data

# robot_ros2 repos 경로 (ROS2 관련 패키지들)
ROBOT_ROS2_REPOS_PATH=D:/Data_AR/dev/multi-hri-mr/robot_ros2/repos

# 호스트 IP (외부 접근용, HoloLens 등에서 접속할 IP)
# 기본값: 0.0.0.0 (모든 인터페이스)
# 예시: 192.168.0.100
HOST_IP=0.0.0.0
