# Multi-HRI AI Server 환경 설정
#
# 이 파일을 .env로 복사 후 로컬 경로 수정
# cp .env.example .env

# shared_data 경로 (config 폴더 포함)
# Windows: D:/Data_AR/dev/multi-hri-mr/shared_data
# Linux: /home/user/multi-hri-mr/shared_data
SHARED_DATA_PATH=D:/Data_AR/dev/multi-hri-mr/shared_data

# Ollama 호스트 (GPU PC에서 실행 중인 경우)
# 로컬: host.docker.internal:11434
# 원격: 192.168.1.100:11434
OLLAMA_HOST=host.docker.internal:11434
